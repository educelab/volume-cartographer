name: Unit tests
on:
  workflow_dispatch:
  push:
    branches: ["develop"]
  pull_request:
    branches:
      - "develop"

concurrency:
  group: "${{ github.ref }}"
  cancel-in-progress: true
jobs:
  debian:
    name: Debian
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - version: "12.2"
          - version: "13.0"
        build:
          - type: "static"
          - type: "dynamic"
            cmake_flags: "-DBUILD_SHARED_LIBS=ON"
    container: ghcr.io/educelab/ci-docker:${{ matrix.build.type }}.${{ matrix.image.version }}
    timeout-minutes: 180
    env:
      EXTRA_CMAKE_FLAGS: ${{ matrix.build.cmake_flags }}
      CMAKE_PREFIX_PATH: "/usr/local/Qt-6.10.0/"
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    # smgl and libcore aren't packaged for apt or bundled in ci-docker, so build
    # them from source for find_package(). Pass EXTRA_CMAKE_FLAGS (which carries
    # BUILD_SHARED_LIBS for the dynamic matrix build) so their linkage matches the
    # vc_core we're about to build against them.
    - name: Install smgl and libcore
      run: |
        apt-get update
        apt-get install -y --no-install-recommends nlohmann-json3-dev
        git clone --quiet --depth 1 --branch v0.11.0-rc.2 https://github.com/educelab/smgl.git /tmp/smgl
        cmake -S /tmp/smgl -B /tmp/smgl/build -GNinja -DCMAKE_BUILD_TYPE=Release $EXTRA_CMAKE_FLAGS \
          -DSMGL_BUILD_JSON=OFF -DSMGL_USE_BOOSTFS=OFF -DSMGL_BUILD_TESTS=OFF -DSMGL_BUILD_DOCS=OFF
        cmake --build /tmp/smgl/build && cmake --install /tmp/smgl/build
        git clone --quiet --depth 1 --branch v0.3.0-rc.1 https://github.com/educelab/libcore.git /tmp/libcore
        cmake -S /tmp/libcore -B /tmp/libcore/build -GNinja -DCMAKE_BUILD_TYPE=Release $EXTRA_CMAKE_FLAGS \
          -DEDUCE_CORE_BUILD_TESTS=OFF -DEDUCE_CORE_BUILD_DOCS=OFF -DEDUCE_CORE_BUILD_EXAMPLES=OFF
        cmake --build /tmp/libcore/build && cmake --install /tmp/libcore/build
        ldconfig

    - name: Build volume-cartographer
      run: |
        cmake -S . -B build/ -GNinja -DVC_BUILD_EXAMPLES=ON -DCMAKE_BUILD_TYPE=RelWithDebInfo -DVC_BUILD_TESTS=ON $EXTRA_CMAKE_FLAGS
        cmake --build build/

    - name: Run tests
      run: ctest -V --test-dir build/

  macOS:
    name: macOS
    strategy:
      matrix:
        os:
          - version: "15"
            exclude_sdk: "14"
          - version: "26"
            exclude_sdk: "{14..15}"
    runs-on: macos-${{ matrix.os.version }}
    timeout-minutes: 180
    env:
      EXTRA_CMAKE_FLAGS: "-DVC_BUILD_EXAMPLES=ON -DCMAKE_BUILD_TYPE=RelWithDebInfo -DVC_BUILD_TESTS=ON"
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Use the Command Line Tools
        run: |
          sudo xcode-select -s /Library/Developer/CommandLineTools

      - name: Remove problematic SDKs
        run: |
          sudo rm -rf /Library/Developer/CommandLineTools/SDKs/MacOSX${{ matrix.os.exclude_sdk }}*.sdk

      # Homebrew's fmt 12.2.0 broke compilation against spdlog (a Brewfile
      # dependency). Pin fmt to the last compatible release until spdlog
      # supports fmt 12.2.0. Tracking issue: educelab/volume-cartographer#147
      - name: Pin fmt to 12.1.0 (workaround for spdlog/fmt 12.2.0 incompatibility)
        run: ./scripts/brew-pin-fmt.sh

      - name: Install dependencies
        run: |
          # Trust required for Homebrew to load this third-party tap non-interactively.
          brew tap educelab/code
          brew trust educelab/code
          brew bundle
          brew install ninja

      - name: Build volume-cartographer
        run: |
          cmake -S . -B build/ -GNinja $EXTRA_CMAKE_FLAGS
          cmake --build build/

      - name: Run tests
        run: ctest -V --test-dir build/