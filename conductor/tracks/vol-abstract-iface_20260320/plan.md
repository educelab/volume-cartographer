# Implementation Plan: Volume Abstract Interface

**Track ID:** vol-abstract-iface_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

Split `Volume.hpp/.cpp` into a pure virtual base (`Volume`) and a concrete TIFF implementation (`TIFFVolume`). Update all downstream call sites to use the base type. No behavior changes.

## Phase 1: Audit & Test Baseline

Establish a complete picture of the current `Volume` public API and all call sites before touching any code.

### Tasks

- [ ] Task 1.1: Read `core/include/vc/core/types/Volume.hpp` and `core/src/Volume.cpp` in full; document every public method and which are implementation-specific vs. logically part of any volume backend
- [ ] Task 1.2: Grep all call sites across `apps/`, `utils/`, `texturing/`, `segmentation/`, `graph/`, `python/` that use `Volume::`, `Volume::Pointer`, or `vol->` to get the full change surface
- [ ] Task 1.3: Run existing test suite (`ctest -V --test-dir build/ -R vc_core`) and record baseline pass/fail
- [ ] Task 1.4: Write a new test `vc_core_VolumeInterfaceTest` that exercises only the methods that must exist on the abstract base (metadata, sampling) — this test will be written against `Volume::Pointer` and must compile against both `TIFFVolume` and any future backend

### Verification

- [ ] Baseline tests pass
- [ ] New interface test compiles and passes against current `Volume` (pre-split)

## Phase 2: Define Abstract Base Class

### Tasks

- [ ] Task 2.1: Create `core/include/vc/core/types/IVolume.hpp` (or rename existing header) with pure virtual interface — `sliceWidth()`, `sliceHeight()`, `numSlices()`, `voxelSize()`, `bounds()`, `getSliceData()`, `getSliceDataCopy()`, `intensityAt()`, `interpolateAt()`, `reslice()` — all `= 0`
- [ ] Task 2.2: Decide (and document) whether cache-control methods (`setCacheSize`, `setCacheMemoryInBytes`) belong on the base or only `TIFFVolume`; default recommendation: base provides no-op virtual defaults, `TIFFVolume` overrides
- [ ] Task 2.3: Ensure `Volume::Pointer = std::shared_ptr<Volume>` and `Volume::New(...)` factory pattern are defined on the base

### Verification

- [ ] Abstract base compiles in isolation (no link needed yet)
- [ ] Interface test from Phase 1 compiles against the new base

## Phase 3: Implement TIFFVolume

### Tasks

- [ ] Task 3.1: Rename/create `core/include/vc/core/types/TIFFVolume.hpp` and `core/src/TIFFVolume.cpp`
- [ ] Task 3.2: Move all TIFF/LRU/mmap implementation from `Volume.cpp` into `TIFFVolume`; have it inherit from `Volume`
- [ ] Task 3.3: Keep `Volume.hpp` as a convenience include that includes both base and `TIFFVolume` headers (backwards-compat shim — to be removed after call-site update)
- [ ] Task 3.4: Update `VolumePkg` to construct `TIFFVolume` instances and return them as `Volume::Pointer`
- [ ] Task 3.5: Update CMakeLists for `vc_core` to compile `TIFFVolume.cpp`

### Verification

- [ ] All `vc_core_*` tests pass
- [ ] `VolumePkg` tests pass
- [ ] No functional change in `vc_render` smoke test

## Phase 4: Update All Call Sites

### Tasks

- [ ] Task 4.1: Update `texturing/` — all algorithms use `Volume::Pointer` (already likely correct if they use the pointer type, but verify no casts to concrete type)
- [ ] Task 4.2: Update `segmentation/` call sites
- [ ] Task 4.3: Update `apps/` and `utils/` call sites
- [ ] Task 4.4: Update `graph/` node wrappers
- [ ] Task 4.5: Update Python bindings if `VC_BUILD_PYTHON_BINDINGS` is enabled
- [ ] Task 4.6: Remove the backwards-compat shim in `Volume.hpp` (or keep if it genuinely has no concrete content left)

### Verification

- [ ] Full test suite passes: `ctest -V --test-dir build/`
- [ ] Project compiles with `-Wall -Wextra` without new warnings
- [ ] `git clang-format develop --diff` shows no formatting violations

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] No call site directly constructs `TIFFVolume` except `VolumePkg` and test utilities
- [ ] `Volume::Pointer` is the universal type used across all algorithm interfaces
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
