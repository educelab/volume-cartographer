# Implementation Plan: Zarr Volume Backend

**Track ID:** vol-zarr-backend_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

Add `z5` as a FetchContent dependency, implement `ZarrVolume` as a `Volume` subclass, wire it into `VolumePkg` auto-detection, and provide a TIFF→Zarr conversion utility.

## Phase 1: Dependency & Skeleton

### Tasks

- [ ] Task 1.1: Evaluate `z5` API for the required operations: read a subarray (arbitrary bounding box), read metadata (array shape, chunk shape, dtype), list chunks; document findings
- [ ] Task 1.2: Add `z5` to `cmake/BuildZ5.cmake` via FetchContent; link it into a new `vc_zarr` INTERFACE or STATIC library target
- [ ] Task 1.3: Create `core/include/vc/core/types/ZarrVolume.hpp` with class declaration inheriting `Volume`; stub out all pure virtual methods with `throw std::runtime_error("not implemented")`
- [ ] Task 1.4: Create `core/src/ZarrVolume.cpp`; add to `vc_core` CMakeLists
- [ ] Task 1.5: Write a compile-only test to confirm `ZarrVolume` satisfies the `Volume` interface (instantiation test, not functional)

### Verification

- [ ] Project compiles with `ZarrVolume` stub
- [ ] `z5` FetchContent fetch succeeds in CI

## Phase 2: Core Implementation

### Tasks

- [ ] Task 2.1: Implement metadata methods: `sliceWidth()`, `sliceHeight()`, `numSlices()` — read from `.zarray` JSON via `z5`
- [ ] Task 2.2: Implement `getSliceData(int z)` — read full XY plane at slice index z by fetching all intersecting chunks and assembling into a `cv::Mat`
- [ ] Task 2.3: Implement `intensityAt(int x, int y, int z)` — fetch the single chunk containing (x,y,z), read the scalar value
- [ ] Task 2.4: Implement `interpolateAt(double x, double y, double z)` — fetch the (up to) 2×2×2 chunk neighborhood and trilinearly interpolate; reuse the same interpolation math as `TIFFVolume`
- [ ] Task 2.5: Implement `reslice(...)` — can delegate to a sequence of `interpolateAt` calls (same approach as `TIFFVolume`)
- [ ] Task 2.6: Write `vc_core_ZarrVolumeTest`: generate a small synthetic Zarr volume (known values), exercise all interface methods, assert results match expected values
- [ ] Task 2.7: Write a round-trip test: convert a small test TIFF volume to Zarr (using the conversion utility from Phase 3), then compare `interpolateAt` results between `TIFFVolume` and `ZarrVolume` on the same coordinates

### Verification

- [ ] `vc_core_ZarrVolumeTest` passes
- [ ] Round-trip test passes (pixel-exact for integer coords, tolerance ≤ 1 intensity unit for interpolated)

## Phase 3: Conversion Utility

### Tasks

- [ ] Task 3.1: Create `utils/src/ConvertVolume.cpp` (CLI: `vc_convert_volume`) — reads a `.volpkg` TIFF volume and writes a Zarr array into the same volume directory
- [ ] Task 3.2: Conversion parameters: `--chunk-size` (default 128), `--compressor` (blosc/zlib/raw, default blosc), `--threads` (parallel chunk writes, default nproc/2)
- [ ] Task 3.3: Write a conversion test using the Testing.volpkg fixture; assert output Zarr has correct shape and values
- [ ] Task 3.4: Update `vc_packager` or add a `--zarr` flag to it as an alternative to the standalone utility (decide which is more ergonomic)

### Verification

- [ ] `vc_convert_volume --help` works
- [ ] Conversion test passes
- [ ] Converted volume passes round-trip test from Phase 2

## Phase 4: VolumePkg Integration & Prefetch

### Tasks

- [ ] Task 4.1: Update `VolumePkg::volume()` to detect `volumes/{id}/zarr/.zarray` and construct `ZarrVolume` instead of `TIFFVolume`; fall back to TIFF if no Zarr found
- [ ] Task 4.2: Implement `ZarrVolume::prefetch(std::span<const int> sliceIndices)` — submit chunk fetches for all chunks intersecting the given slice range to a background thread pool (reuse same pattern as `TIFFVolume::prefetch`)
- [ ] Task 4.3: Run `vc_render` and `vc_generate_ppm` end-to-end with a Zarr-backed `.volpkg` (manual smoke test); output must match TIFF-backed run within tolerance
- [ ] Task 4.4: Update `vc_core_VolumePkgTest` to cover Zarr auto-detection
- [ ] Task 4.5: Full test suite: `ctest -V --test-dir build/`
- [ ] Task 4.6: `git clang-format develop` pass

### Verification

- [ ] All acceptance criteria in spec.md met
- [ ] VolumePkg auto-detection test passes
- [ ] End-to-end smoke test output matches TIFF reference
- [ ] Ready for review

## Final Verification

- [ ] `ZarrVolume` pixel-exact (or within tolerance) against `TIFFVolume` on all interface methods
- [ ] Conversion utility tested and documented in CLI `--help`
- [ ] No existing tests broken
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
